/* Chiffres affichés sur la page d'accueil.
   À mettre à jour avec data/rules-fr.json (champ `ruleCount` de chaque document)
   à chaque nouvelle version des règles — le JSON complet est trop lourd pour être
   téléchargé juste pour ces deux nombres. */
export const RULE_COUNTS = {
  core: 2137, // règles du jeu
  tournament: 812 // règles de tournoi
}
